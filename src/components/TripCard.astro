---
import { Image } from "astro:assets";
const { href, title, description, img, alt } = Astro.props;
---

<div class="trip-card">
	<a href={href}>
	<Image src={img} alt={alt} width='300px' />
  <p class="card-title">{title}</p>
	<p>{description}</p>
</div>

<style>
	.trip-card {
		width: 300px;
		margin-bottom: 40px;
	}
	img {
		border-radius: 10px;
	}
	a {
		color: var(--lightColor);
	}
	a::before {
		display: none;
	}
	p {
		margin: 4px 0;
	}
	.card-title {
		font-family: 'Onest', sans-serif;
		font-weight: 500;
		font-size: 1.2rem;
	}

</style>