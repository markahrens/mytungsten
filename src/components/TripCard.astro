---
import { Image } from "astro:assets";
const { href, title, description, img, alt } = Astro.props;
---

<div class="trip-card card card--interactive">
  <div class="image-container">
    <Image src={img} alt={alt} width={600} loading="lazy" sizes="(max-width: 600px) 100vw, 600px" class="trip-card-image" />
  </div>
  <div class="card-content">
    <h2 class="card-title"><a href={href} class="stretched-link">{title}</a></h2>
    <p class="card-description">{description}</p>
  </div>
</div>

<style>
  .trip-card {
    display: flex;
    flex-direction: column;
    height: 100%;
    
    .image-container {
      aspect-ratio: 16 / 10;
      overflow: hidden;
      border-bottom: 1px solid var(--color-border);
    }
    
    .trip-card-image {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform var(--transition-standard);
    }
    
    &:hover .trip-card-image {
      transform: scale(1.03);
    }
    
    .card-content {
      padding: var(--space-6);
      display: flex;
      flex-direction: column;
      gap: var(--space-3);
      flex: 1;
    }
    
    .card-title {
      font-size: 1.4rem;
      font-weight: 700;
      line-height: 1.2;
      color: var(--color-text);
      margin: 0;
      
      a {
        text-decoration: none;
        color: inherit;
      }
    }
    
    .card-description {
      font-size: 0.95rem;
      line-height: 1.5;
      color: var(--color-metallic);
      margin: 0;
    }
  }
</style>
